/**
 * Sokuza package version. Kept in sync with package.json on release.
 * Referenced by the /health endpoint and persisted runtime state so the
 * public discovery page can verify it is talking to a known app version.
 */
export const VERSION = '0.1.0';
